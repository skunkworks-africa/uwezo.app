specVersion: "v1"
serviceId: "studio"
location: "africa-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "mysql"
      cloudSql:
        instanceId: "sqladmin"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
